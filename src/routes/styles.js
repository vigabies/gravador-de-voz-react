import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  botao: {
    borderRadius: 6,
    height: 30,
    width: 85,
  },

  text: {
    color: '#fff',
    fontSize: 18,
    textAlign: 'center',
    fontWeight: '500',
  },
});
export default styles;
